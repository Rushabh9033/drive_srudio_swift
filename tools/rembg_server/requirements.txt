# Drive Studio — local rembg cutout demo (CPU)
# Install:  pip install -r requirements.txt
# Run:      uvicorn main:app --host 127.0.0.1 --port 8787

rembg[cpu]>=2.0.57
pillow>=10.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
